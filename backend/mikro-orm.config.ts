/**
 * Root-level MikroORM config for CLI usage (migrations, seeding).
 * Re-exports the config from src/config/ so the CLI can find it.
 */
export { default } from './src/config/mikro-orm.config.js';
