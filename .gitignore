node_modules/
.superpowers/
.worktrees/
nul
frontend/nul

# Local env files
.env
.env.*
!.env.example

# Claude Code transient runtime lock (per-process; not source)
.claude/scheduled_tasks.lock
# Local artifact (résumé .tex export test)
/example_resume.tex

.claude/worktrees/

# Playwright CLI scratch output (screenshots, logs, page dumps)
.playwright-cli/

# Expo mobile app — Metro/prebuild artifacts. ios/ and android/ are regenerated
# by `expo prebuild`; committing them fights the config plugins.
mobile/node_modules/
mobile/.expo/
mobile/ios/
mobile/android/

# Local RPM and IDE artifacts
*.rpm
.idea/
mobile/.idea/
.codex/
